#include "header.h"

int main()
{
    doit(new Bar());
    doit(new Baz());
    doit(new OtherBaz());
    doit(new Blah());
    doit(new OtherBlah());
    doit(new Stuff());
    doit(new OtherStuff());
    return 0;
}


